fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.28
pydantic>=2.11
python-multipart>=0.0.20
tzdata>=2024.1; sys_platform == "win32"

# Optional vision stack — backend works without these (falls back to time-of-day heuristic)
# ultralytics==8.3.40
# opencv-python-headless==4.10.0.84
# numpy==2.1.3
