/**
 * Fallback guide copy — demo survives without AI/API.
 * Source of truth: landmarks.ts messages per location & language mode.
 */
export { landmarks as fallbackLandmarks } from './landmarks';
export type { Language, Landmark } from './landmarks';
